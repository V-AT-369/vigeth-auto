[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -u fetch_tenders.py"
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
cronSchedule = "30 0 * * *"
